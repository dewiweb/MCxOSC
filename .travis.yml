dist: bionic
language: node_js
matrix:
  include:
    - os: linux
      language: node_js
      node_js: "12"
cache:
  directories:
    - node_modules
    - $HOME/.cache/electron
    - $HOME/.cache/electron-builder
before_script:
  - yarn add electron-builder --dev
script:
  - npm run dist 



